server:
  port: 9003

spring:
  application:
    name: credit-service

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        cluster-name: FZ
        metadata:
          version: v1
  profiles:
    active: dev